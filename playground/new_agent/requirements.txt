# Tibet Watch — install into the `monlam` conda env (Python 3.11)
#   conda activate monlam && pip install -r requirements.txt

# Agent framework. langchain 1.x provides create_agent; langgraph is its runtime.
langchain>=1.3
langchain-core>=1.5
langgraph>=1.2

# Sources and extraction
feedparser>=6.0
trafilatura>=2.0
requests>=2.31

# Tracing. Ships as a langchain-core dependency, pinned here because the
# tracing module depends on process_inputs/process_outputs in @traceable.
langsmith>=0.10

# Demo server
fastapi>=0.115
uvicorn>=0.30

python-dotenv>=1.0
