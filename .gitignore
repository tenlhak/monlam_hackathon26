# Secrets
.env
.env.*
!.env.example
.claude/settings.local.json

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# Cached OCR responses — bulky, regenerable, and billed per page so worth
# keeping locally rather than re-fetching.
tools/.cache/

# Source dictionaries — tens of MB, copyrighted, and regenerable from source.
*.pdf

# TanStack Router build cache
web/.tanstack/

# Local databases
*.db
*.sqlite
*.sqlite3

# News corpus and issue drafts — a cache the crawler rebuilds, not source.
t_tutor/data/

# Editor / OS
.DS_Store
Thumbs.db
